Console program to update the `.vcxproj` and `.vcxproj.filters` of projects in sibling folders:

* removes all references to WinDlg files;
* scans current WinDlg file names;
* adds references to WinDlg files.
